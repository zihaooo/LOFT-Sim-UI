# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example

# Cache / temp
.cache/
.tmp/
.temp/
.vite/
coverage/
*.tsbuildinfo

# Assets — keep dir structure, ignore contents
public/data/**
!public/data/**/
!public/data/**/.gitkeep
# The UAV models are UI assets (small, stable) and must ship in the build.
!public/data/model/*.gltf
# Per-type drone shadow profiles (computed by scripts/compute_shadow_params.py) are imported at build
# time by src/layer/shadowProfiles.ts, so the file must be committed or a fresh build can't resolve it.
!public/data/model/shadow_profiles.json

# Python
__pycache__/
*.py[cod]

# Performance profiling
profile
mock

context
